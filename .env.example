# AI_Powered_Testing_Agent - Security Configuration

# ========================================
# APPLICATION CREDENTIALS (Local Only)
# ========================================
# These are NEVER sent to any cloud service
APP_USERNAME=standard_user
APP_PASSWORD=secret_sauce
APP_LOGIN_URL=https://www.saucedemo.com

# ========================================
# LLM PROVIDER KEYS (Cloud Authentication)
# ========================================
# IMPORTANT: Only ONE provider needed - comment out others
# These keys are sent ONLY to their respective providers

# Option 1: Google Gemini (Recommended)
# Get your key: https://aistudio.google.com/apikey
PROVIDER=gemini
GOOGLE_API_KEY=your_google_api_key_here

# Option 2: OpenAI
#PROVIDE R=openai
#OPENAI_API_KEY=your_openai_api_key_here

# Option 3: Anthropic Claude
#PROVIDER=anthropic
#ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ========================================
# SECURITY & COMPLIANCE (Optional)
# ========================================

# Enable audit logging for LLM requests (for enterprise compliance)
# Set to 'true' to log all LLM interactions and detect credential leaks
# Default: false (disabled for performance)
# Recommended for production: true
ENABLE_AUDIT_LOG=false

# When enabled, audit logs are saved to: data/security_audit/*.jsonl
# Compliance reports generated to: data/security_audit/compliance_report_*.md

# To verify security:
# 1. Set ENABLE_AUDIT_LOG=true
# 2. Run tests
# 3. Check: grep -i "password" data/security_audit/*.jsonl
#    (Should return NOTHING - proves credentials never sent to LLM)
